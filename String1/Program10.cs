/*using System;

class Program{
	static void RemoveChar(string str, char remove){
		string ans = "";
		foreach(char ch in str) if(ch != remove || ch != remove) ans += ch;
		Console.WriteLine($"String after Modification {ans}");
	}
	static void Main(){
		string str = Console.ReadLine();
		char remove = Convert.ToChar(Console.ReadLine());
		RemoveChar(str, remove);	
	}
}
*/