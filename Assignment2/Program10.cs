/*using System;

class SumOfNumbers
{
    static void Main(string[] args)
    {
        double total = 0.0;  // Variable to store the sum of numbers
        double userInput;     // Variable to store the user's input

        // Loop to keep asking for input until user enters 0
        while (true)
        {
            Console.WriteLine("Enter a number (or enter 0 to stop):");
            userInput = Convert.ToDouble(Console.ReadLine());

            // If the user enters 0, break out of the loop
            if (userInput == 0)
            {
                break;
            }

            // Add the entered number to the total
            total += userInput;
        }

        // Display the total sum
        Console.WriteLine($"Total sum: {total}");
    }
}
*/