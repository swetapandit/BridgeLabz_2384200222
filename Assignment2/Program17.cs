/*using System;

public class Program17
{
    public static void Main(string[] args)
    {
        Console.Write("Enter the employee's salary: ");
        double salary = Convert.ToDouble(Console.ReadLine());

        Console.Write("Enter the employee's years of service: ");
        int yearsOfService = Convert.ToInt32(Console.ReadLine());

        double bonus = CalculateBonus(salary, yearsOfService);
	Console.WriteLine($"Bonus amount: {bonus}");
    }

    static double CalculateBonus(double salary, int yearsOfService)
    {
        if (yearsOfService > 5) return salary * 0.05;
	else return 0;
    }
}*/