// using System;

// class Program1
// {
//     // Non-static function to check if the person can vote
//     public bool CanVote(int age)
//     {
//         return age >= 18;  // A person can vote if their age is 18 or older
//     }

//     static void Main()
//     {
//         // Take the age as input
//         int age = Convert.ToInt32(Console.ReadLine());

//         // Create an instance of Program1 to call the non-static method
//         Program1 program = new Program1();

//         // Check if the person can vote and print the result
//         if (program.CanVote(age)) Console.WriteLine($"The person's age is {age} and can vote.");
//         else Console.WriteLine($"The person's age is {age} and cannot vote.");
//     }
// }
