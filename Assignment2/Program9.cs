/*using System;

class RocketLaunch
{
    // Function to perform the countdown
    static void Countdown(int counter)
    {
        // Using for-loop to countdown
        for (int i = counter; i >= 1; i--)
        {
            Console.WriteLine($"Countdown: {i}");
        }
    }

    static void Main(string[] args)
    {
        // Take user input for the countdown value
        Console.WriteLine("Enter the countdown value for the rocket launch:");
        int counter = Convert.ToInt32(Console.ReadLine());

        // Call the Countdown function
        Countdown(counter);
    }
}
*/