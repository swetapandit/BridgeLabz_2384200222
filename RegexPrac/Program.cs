//using System;
//using System.Text.RegularExpressions;
//class Program
//{
//    static void Main()
//    {
        //string pattern = @"\b\d+\b"; // Matches one or more digits
        //string input = "The price 900 45 is 200 dollars";

        // Match match = Regex.Match(input, pattern);
        //if (match.Success)
        //{
        //    Console.WriteLine("Match found: " + match.Value);
        //}
        //MatchCollection matches = Regex.Matches(input, pattern);

        //foreach (Match match in matches)
        //{
        //    Console.WriteLine("Number: " + match.Value);
        //}




        //string pattern = @"\b\w+\b"; // Matches words in a string
        //string input = "Hello, World!";
        //MatchCollection matches = Regex.Matches(input, pattern);

        //foreach (Match match in matches)
        //{
        //    Console.WriteLine("Word: " + match.Value);
        //}

        //string pattern = @"[A-Z][a-z]+"; // Matches capitalized words
        //string input = "This is a Test String";

        //MatchCollection matches = Regex.Matches(input, pattern);
        //foreach (Match match in matches)
        //{
        //    Console.WriteLine(match.Value);
        //}

        //string pattern = @"\d{2,4}"; // Matches numbers with 2 to 4 digits
        //string input = "Year: 2023, Code: 43, ID: 12345";

        //MatchCollection matches = Regex.Matches(input, pattern);
        //foreach (Match match in matches)
        //{
        //    Console.WriteLine(match.Value);
        //}

        //string pattern = @"\d+(?= dollars)"; // Matches digits followed by "dollars"
        //string input = "The price is 100 dollars.";

        //Match match = Regex.Match(input, pattern);
        //if (match.Success)
        //{
        //    Console.WriteLine("Matched price: " + match.Value);
        //}

        //string input = "My phone number is 123-456-7890.";
        //string pattern = @"\d{3}-\d{3}-\d{4}";
        //string replacement = "***-***-****";

        //string result = Regex.Replace(input, pattern, replacement);
        //Console.WriteLine(result); // Output: My phone number is ***-***-****.


//        Console.ReadLine();
//    }
//}



