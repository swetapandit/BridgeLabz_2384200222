/*using System;

class Program
{
    // Function to find no. of digits in integer
    static int NoOfDigits(int num)
    {
	 int cnt = 0;
        while(num>0){
		num/=10;
		cnt++;	
	}
	return cnt;
    }

    // Main function to take input and display the result
    static void Main()
    {
        Console.Write("Enter a number: "); // Prompt the user to enter a number
        int num = Convert.ToInt32(Console.ReadLine()); // Read and convert the input to an integer

        Console.WriteLine($"Number of digits in {num} is " + NoOfDigits(num));
    }
}*/
