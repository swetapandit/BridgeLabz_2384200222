/*using System;

class Program10
{
    // Method to calculate chocolates distribution
    public void DistributeChocolates()
    {
        // Take inputs for the number of chocolates and the number of children
        int numberOfChocolates = Convert.ToInt32(Console.ReadLine());
        int numberOfChildren = Convert.ToInt32(Console.ReadLine());

        // Calculate the number of chocolates each child gets
        int chocolatesPerChild = numberOfChocolates / numberOfChildren;

        // Calculate the number of remaining chocolates
        int remainingChocolates = numberOfChocolates % numberOfChildren;

        // Print the results
        Console.WriteLine($"The number of chocolates each child gets is {chocolatesPerChild} and the number of remaining chocolates is {remainingChocolates}");
    }

    static void Main()
    {
        // Create an instance of Program10 and call the method
        Program10 program = new Program10();
        program.DistributeChocolates();
    }
}
*/