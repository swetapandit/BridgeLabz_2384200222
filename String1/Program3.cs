/*using System;

class Program{
	static bool CheckPalin(string str){
		string rev="";
		for(int i = str.Length-1; i>=0; i--) {
			rev+=str[i];
		}
       	return str == rev;
}

	static void Main(){
		string str = Console.ReadLine();
		if(CheckPalin(str)) Console.WriteLine($"{str} is Palindrom");
		else Console.WriteLine($"{str} is not Palindrom");
	}
}
*/